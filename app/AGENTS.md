Keep all page implementations aligned with QuickHeatFix standards:
- Use `lib/waterheater-data.ts` as the source of truth.
- Keep metadata canonicalized and schema-enabled.
- Keep every CTA tap-friendly and call-focused.
- Use `qhf-*` classes only for new UI work.
